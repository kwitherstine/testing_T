---
- hosts: all
  become: yes
  user: cumulus
  roles:
    - common
  handlers:
    - include: handlers/main.yml

- hosts: access
  become: yes
  user: cumulus
  roles:
    - common
  handlers:
    - include: handlers/main.yml

- hosts: spines
  become: yes
  user: cumulus
  roles:
    - common
  handlers:
    - include: handlers/main.yml

- hosts: leafs
  become: yes
  user: cumulus
  roles:
    - common
  handlers:
    - include: handlers/main.yml

- hosts: servers
  become: yes
  user: cumulus
  roles:
    - servers
  handlers:
    - include: handlers/main.yml